# The deployed SPA's own origin -- /report/{orgId}/{projectId}/{rev} must
# resolve there to index.html (CLAUDE.md, "server route" note in
# reportPdf.ts). Not derivable from the Firebase project id; set this to
# wherever the built dist/ actually gets served.
APP_URL=https://tendermaster-ai.web.app
